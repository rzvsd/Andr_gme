# 🔤 fonts/

Custom game fonts in WOFF2 format for fast loading.

## Expected Files

- `press_start.woff2` — Pixel-art style font for HUD and scores (free, SIL license)

## Usage

Loaded in `index.html` via `@font-face` CSS rule, then used in Canvas via `ctx.font`.
